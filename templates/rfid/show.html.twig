{% extends 'base.html.twig' %}

{% block title %}Rfid{% endblock %}

{% block body %}
    <h1>Rfid</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rfid.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ rfid.number }}</td>
            </tr>
            <tr>
                <th>Key_code</th>
                <td>{{ rfid.keyCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rfid_index') }}">back to list</a>

    <a href="{{ path('app_rfid_edit', {'id': rfid.id}) }}">edit</a>

    {{ include('rfid/_delete_form.html.twig') }}
{% endblock %}
