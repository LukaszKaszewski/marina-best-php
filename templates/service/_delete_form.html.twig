<form method="post" action="{{ path('app_service_delete', {'id': service.id}) }}" onsubmit="return confirm('Jesteś pewien że chcesz skasować ten wpis?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="contact__form-btn btn-special-animation spacer">Usuń wpis</button>
</form>
