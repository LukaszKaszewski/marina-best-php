<form method="post" action="{{ path('app_wintering_delete', {'id': wintering.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wintering.id) }}">
    <button class="btn">Skasuj</button>
</form>
